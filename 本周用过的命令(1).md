##  Git 本周命令回顾学习

### 1.初始化配置

- git config  --global  user.name "用户名" ：配置用户名（--global参数表示的是全局配置，所有仓库生效）
- git config  --global  user.email  邮箱：配置邮箱
- git config  --global  credential.helper  store : 保存用户名和密码
- git config --global  --list : 列出当前用户的全局Git配置项（查看Git的配置信息）

### 2.新建仓库

- git init  : 创建仓库（创建的是一个由Git管理的、能追踪所有变更历史的特殊仓库）

- mkdir :  创建目录（文件夹，只是创建一个普通文件夹）

- cd  : 切换目录（进入某个文件夹）

- git init : 初始化一个新的Git仓库

- ls -a l  ：以长格式列出当前目录下的所有文件（包括隐藏文件）

- ls -ltr  :按修改时间从旧到新列出文件，最新的排在最后面。

- ls -a   :列出当前目录下的所有文件，包括隐藏文件

### 3.添加和提交文件

- git status : 查看仓库状态

- git add   :  添加到暂存区

- git commit : 提交到仓库（只提交暂存区的内容，不会提交工作区中的内容）

- git log  :  查看仓库提交历史记录

- cat  : 查看文件内容（ls查看目标结构）

- git reset HEAD^  ：把当前分支的指针从最新提交，往回退一步，退到上一次提交。（git reset 重置/回退命令）

- git log --oneline : 以精简的一行格式显示提交历史，每条提交只占一行，只显示简短的提交哈希值和提交信息。

### 4.远程仓库同步

- git remote -v  ：查看当前仓库关联的所有远程仓库及远程仓库地址

- git remote add origin <远程仓库地址> ： 将本地仓库与GitHub等远程仓库进行关联

- git push -u origin master : 将本地的提交推送到远程仓库。（-u参数会在首次推送时建立本地分支与远程分支的关联）

- git pull : 从远程仓库拉取最新的代码更新，并自动合并到本地

### 5.克隆仓库

- git clone : 克隆命令（从远程服务器下载整个仓库）
- git clone -b dev : 克隆指定分支
- git clone --depth 1  : 浅克隆（只下载最近1次提交，适合大仓库，速度快）
